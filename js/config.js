/**
 * Frontend runtime configuration.
 *
 * Default is an empty string: the page and the solver API share an origin
 * (the FastAPI container serves both). Set to an absolute URL like
 * "http://lab.example.com:8000" if you're hosting the frontend separately.
 */

export const SOLVER_URL = '';
